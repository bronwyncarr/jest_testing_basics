describe("Calculator", () => {
  describe(".add()", () => {
    it("adds number b and number a", () => {});

    it.todo("throws and error if not passed numbers");

    it("handles negative numbers", () => {});
  });

  describe(".subtract()", () => {
    it("subtracts number b from number a", () => {
      expect(true).toBe(false);
    });

    it("throws and error if not passed numbers", () => {});

    it.skip("handles negative numbers", () => {});
  });
});
